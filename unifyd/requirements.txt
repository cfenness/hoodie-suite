# Unifyd MDM — local agent + scraper dependencies
# install:  pip install -r requirements.txt

flask>=3.0
requests>=2.31
beautifulsoup4>=4.12
boto3>=1.34          # only used when STATE_BUCKET is set (S3-backed state); unused in local disk mode
anthropic>=0.69      # only used when AGENT_SELF_HEAL=1 (LLM column-remap self-heal); unused otherwise
duckdb>=1.5          # query Parquet in place (Tigris/local warehouse); lazy-imported. The floor is 1.5,
                     # not 1.0: raw_capture.evacuate() streams via `.to_arrow_reader()`, which does not
                     # exist before 1.5 (measured: 1.4.5 has only fetch_record_batch). Every such import
                     # is lazy, so an older resolve compiles, deploys green, and AttributeErrors the
                     # first time evacuate actually runs — the same swallowed-import class as the
                     # playwright/patchright break. Fly currently resolves 1.5.5.
pyarrow>=15.0        # places connector: write Parquet; lazy-imported (only when a pull runs)
PyYAML>=6.0          # serve the OpenAPI spec at /api/openapi.json; lazy-imported

# Optional — only needed to land Iowa Liquor Sales from the BigQuery public mirror (iowa_bq.py).
# Lazy-imported; uncomment + set GCP_SA_JSON / GCP_PROJECT secrets to activate. Heavy (grpc/protobuf),
# so kept off the default image until Iowa is turned on.
# google-cloud-bigquery>=3.20

# COLA label enrichment (ttb-enrich): pyzbar decodes the BARCODE → UPC; pytesseract reads the label TEXT →
# gov warning / claims (ttb_cola_labels.read_label_text). Both bind system libs the Dockerfile installs
# (libzbar0 / tesseract-ocr); pillow loads the image. All no-op cleanly if a lib is missing.
pyzbar>=0.1.9
pytesseract>=0.3.10
pillow>=10.0
# pylibdmtx: zbar does NOT implement DataMatrix (ZBarSymbol has no DATAMATRIX member — confirmed on the
# deployed image), and GS1's Sunrise 2027 2D codes are QR *or* GS1 DataMatrix. pyzbar covers the QR half;
# this covers the other. Binds to the libdmtx0 system lib (added in the Dockerfile alongside libzbar0).
pylibdmtx>=0.1.10
# setuptools is a HARD requirement of pylibdmtx on this image, not a build-time nicety: the runtime is
# Python 3.12, which REMOVED distutils, and pylibdmtx/wrapper.py does `from distutils.version import
# LooseVersion`. Without setuptools (which re-provides distutils via its shim) the import dies with
# "No module named 'distutils'" — the system libdmtx.so.0 loads fine, so the failure is purely the
# Python side and is invisible until it runs on the image. Verified live on Fly before pinning here.
setuptools>=68
cryptography
openpyxl

# Snowflake morning drop (registry build `snowflake-load` → snowflake/run_load.py): lazy-imported,
# only used when the SNOWFLAKE_* secrets are set. Key-pair auth reuses `cryptography` above.
snowflake-connector-python>=3.7
# DAM press-release facts (dam.document_text -> brand_events): pure-Python PDF text extraction, no
# compiled deps. OPTIONAL and capability-probed — without it PDF press releases contribute no facts
# and the run says so (a source that reads 0 of 91 PDFs must not look like a source with no PDFs).
# DOCX needs nothing: it is a zip of XML, handled with stdlib.
pypdf>=4.0
curl_cffi>=0.7        # Chrome-JA3 TLS impersonation for walmart_direct (PerimeterX reads urllib's TLS as a bot tell)

# Instacart (free, no bd): a self-hosted Chromium drives Instacart's own SearchResultsPlacements GraphQL.
# The Docker image installs this + the bundled Chromium (see Dockerfile). Lazy-imported in instacart.py, so
# it's optional for local dev — only needed to run the Instacart pull. Runs HEADLESS (BROWSER_HEADFUL=0),
# proven to land data from a bare datacenter IP. Install locally with:
#   pip install playwright && playwright install chromium
# playwright>=1.40
