# Extra dep for run_load.py (the Fly-machine load runner). pyarrow + duckdb are already in the
# engine (unifyd/requirements.txt) for the --live regen; the connector is the only addition.
snowflake-connector-python>=3.7
